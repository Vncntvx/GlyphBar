# Example Status Module

Import this folder from GlyphBar Settings > Modules > Import Module.

It demonstrates a declarative manifest, a cached snapshot, one copy action, widget metadata, and compact panel rendering.
