import Foundation

/// One-line summary shown in the status bar dropdown / widget header.
struct SummaryProjection: Sendable {
    let title: String
    let subtitle: String
    let systemImage: String
}
